#!/usr/bin/env bash
result=$(swaymsg -r -t get_tree 2>/dev/null | jq -r '.. | objects | select(.type == "floating_con" or .type == "con") | select(.focused == true) | .name' | head -1)
if [ -n "$result" ] && [ "$result" != "null" ]; then
    echo "$result"
fi